<h2><a href="{{ url('page_page_show', { slug: result.slug }) }}">{{ result.title }}</a></h2>
<p>{{ result.content|striptags|raw }}</p>
<p><a href="{{ url('page_page_show', { slug: result.slug }) }}">{{ 'read_more'|trans }}</a></p>
