#!/usr/bin/env bash

set -e

echo Running static analysis ...
vendor/bin/phpstan analyse src --level=5 --no-progress

echo Running unit tests ...
phpdbg -qrr -d memory_limit=-1 bin/phpunit --coverage-html tests/coverage

echo Running functional tests ...
vendor/bin/behat
