#!/bin/sh

set -e

if [ ! -f vendor/bin/security-checker ]; then
    curl -LsS https://get.sensiolabs.org/security-checker.phar -o vendor/bin/security-checker --create-dirs
    chmod +x vendor/bin/security-checker
fi

echo "Running Security Checker ..."
vendor/bin/security-checker security:check
