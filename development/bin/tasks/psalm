#!/bin/sh

set -e

if [ ! -f vendor/bin/psalm ]; then
    curl -LsS https://github.com/vimeo/psalm/releases/download/3.11.4/psalm.phar -o vendor/bin/psalm --create-dirs
    chmod +x vendor/bin/psalm
fi

echo "Running Psalm ..."
vendor/bin/psalm --config=psalm.xml --show-info=false
